[defaults]
inventory = inventory/hosts
remote_user = $(whoami)
private_key_file = ~/.ssh/gcp_ansible_key
host_key_checking = False
stdout_callback = yaml
timeout = 30

[ssh_connection]
ssh_args = -o ControlMaster=auto -o ControlPersist=60s -o StrictHostKeyChecking=no
